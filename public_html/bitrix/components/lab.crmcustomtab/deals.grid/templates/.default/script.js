
BX.ready(function (e) {

   /* $('body').on('click', '', function (e) {
        e.preventDefault();

        if ($(e.target).parents('tr')){
            alert($(e.target).parents('#BOOK_GRID_table tr').data('id'));
        }
    });*/

});
